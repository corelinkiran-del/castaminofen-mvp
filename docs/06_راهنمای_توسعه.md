# راهنمای توسعه و پیاده‌سازی MVP

## ۱. مراحل اجرا

### ۱.۱ Prerequisites
```bash
# Install Node.js 20 LTS
# Install Docker & Docker Compose
# Install Git
```

### ۱.۲ Initial Setup

```bash
# Clone repository
git clone <repo-url>
cd absence-tracker2

# Install dependencies (all workspaces)
yarn install

# Setup environment variables
cp .env.example .env.local

# Start database and services
docker-compose up -d

# Run migrations
yarn run migrate

# Start all services in development
yarn dev
```

---

## ۲. Backend Services Implementation Status

### ۲.۱ Completed Services

#### Auth Service ✅
- Location: `backend/packages/auth-service`
- Features:
  - User registration with validation
  - Email/password login
  - JWT token generation
  - Token refresh mechanism
  - Password strength validation
  - Email format validation
- Endpoints:
  ```
  POST /api/auth/register
  POST /api/auth/login
  POST /api/auth/refresh-token
  POST /api/auth/logout
  ```

### ۲.۲ Services to Complete

#### User Service (Priority: HIGH)
Location: `backend/packages/user-service`

**Endpoints to implement:**
```
GET    /api/users/me               # Get current user
PUT    /api/users/me               # Update profile
GET    /api/users/{id}             # Get user by ID
GET    /api/users/me/library       # Get user's library
POST   /api/users/me/library       # Add to library
DELETE /api/users/me/library/{id}  # Remove from library
GET    /api/users/me/history       # Get listening history
POST   /api/users/{id}/follow      # Follow user
DELETE /api/users/{id}/follow      # Unfollow user
```

**Key Models:**
- User profiles
- User preferences (dark mode, language, etc)
- Library management
- Following relationships

---

#### Content Service (Priority: HIGH)
Location: `backend/packages/content-service`

**Endpoints to implement:**
```
GET    /api/contents/search?q=...     # Full-text search
GET    /api/contents/{id}             # Get content details
GET    /api/contents/{id}/episodes    # Get episodes list
GET    /api/contents/{id}/episodes/{episodeId}  # Get episode details
GET    /api/contents/explore          # Get featured/trending content
```

**Key Features:**
- Content metadata storage
- Episode management
- Full-text search (Elasticsearch)
- Trending/featured content

---

#### Media Service (Priority: HIGH)
Location: `backend/packages/media-service`

**Endpoints to implement:**
```
GET    /api/media/{episodeId}/stream       # Get streaming URL (HLS)
POST   /api/media/{episodeId}/progress     # Track playback progress
POST   /api/media/{episodeId}/complete     # Mark as completed
GET    /api/media/{episodeId}/subtitles    # Get subtitle/transcript
POST   /api/downloads                       # Start download
GET    /api/downloads/{id}                 # Get download status
```

**Key Features:**
- HLS stream URL generation
- Media progress tracking
- Download management
- Adaptive bitrate streaming

---

#### Payment Service (Priority: MEDIUM)
Location: `backend/packages/payment-service`

**Endpoints to implement:**
```
GET    /api/payments/subscription-plans    # Get available plans
POST   /api/payments/subscribe             # Start subscription
POST   /api/payments/verify                # Verify payment
POST   /api/payments/cancel-subscription   # Cancel subscription
POST   /api/payments/purchase              # One-time purchase
```

**Integration Points:**
- Iranian payment gateway
- Subscription management
- Invoice generation

---

#### Creator Service (Priority: MEDIUM)
Location: `backend/packages/creator-service`

**Endpoints to implement:**
```
POST   /api/creators/apply                 # Apply for creator status
POST   /api/creators/contents              # Create content
POST   /api/creators/contents/{id}/episodes  # Upload episode
PUT    /api/creators/contents/{id}/episodes/{episodeId}/publish  # Publish
GET    /api/creators/analytics/{id}        # Get analytics
```

---

#### Notification Service (Priority: MEDIUM)
Location: `backend/packages/notification-service`

**Endpoints to implement:**
```
POST   /api/notifications/push-token       # Register device
GET    /api/notifications                  # Get notifications
PATCH  /api/notifications/{id}/read        # Mark as read
PATCH  /api/notifications/preferences      # Update preferences
```

---

## ۳. Frontend Implementation Status

### ۳.۱ Mobile App (React Native + Expo)

**Setup:**
```bash
cd frontend-mobile
yarn install
yarn dev
```

**Screens to Implement:**
```
Auth Screens:
├── LoginScreen
├── RegisterScreen
└── OnboardingScreen

Main Screens:
├── HomeScreen (Continue Listening)
├── ExploreScreen
├── SearchScreen
├── LibraryScreen
├── PlaylistScreen
├── PlayerScreen
├── CreatorScreen
└── SettingsScreen
```

**Components:**
- MediaPlayer (with player controls)
- ContentCard (podcast/audiobook/video)
- EpisodeList
- SearchBar
- PlaylistManager

---

### ۳.۲ Web Dashboard (Next.js)

**Setup:**
```bash
cd frontend-web
yarn install
yarn dev
```

**Pages to Implement:**
```
/                     # Home/Landing
/auth/login          # Login page
/auth/register       # Register page
/dashboard           # Main dashboard
/dashboard/library   # User library
/dashboard/playlists # User playlists
/dashboard/history   # Listening history
/creator             # Creator dashboard
/creator/upload      # Upload episode
/creator/analytics   # Analytics dashboard
/explore             # Content discovery
/search              # Search page
/content/{slug}      # Content detail page
```

---

## ۴. Database Migrations

### ۴.۱ Initial Schema

Run migrations to create tables:
```bash
yarn run migrate
```

Or manually using psql:
```sql
\c media_platform

-- Run all SQL scripts from docs/database/migrations/
-- 001_users.sql
-- 002_creators.sql
-- 003_contents.sql
-- 004_episodes.sql
-- 005_user_progress.sql
-- ... etc
```

---

## ۵. Environment Setup

### ۵.۱ .env.local File

```bash
# Core
NODE_ENV=development
LOG_LEVEL=debug
PORT=3000

# Services
AUTH_SERVICE_PORT=3001
USER_SERVICE_PORT=3002
CONTENT_SERVICE_PORT=3003
MEDIA_SERVICE_PORT=3004
PAYMENT_SERVICE_PORT=3005

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/media_platform
DATABASE_SSL=false

# Cache
REDIS_URL=redis://localhost:6379/0

# Storage
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=media-platform

# Messaging
RABBITMQ_URL=amqp://guest:guest@localhost:5672

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRATION=3600
REFRESH_TOKEN_EXPIRATION=2592000

# OAuth (add your credentials)
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-secret
APPLE_CLIENT_ID=your-client-id
APPLE_TEAM_ID=your-team-id

# Email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password

# Payments (Iranian gateway)
PAYMENT_GATEWAY_KEY=your-key
PAYMENT_GATEWAY_SECRET=your-secret
```

---

## ۶. Testing Strategy

### ۶.۱ Unit Tests
```bash
# Test Auth Service
yarn workspace @media-platform/auth-service test

# Test all services
yarn test
```

### ۶.۲ Integration Tests
```bash
# Run with docker-compose up
yarn test:integration
```

### ۶.۳ E2E Tests
```bash
# Mobile app (Detox)
cd frontend-mobile
yarn test:e2e

# Web app (Cypress)
cd frontend-web
yarn test:e2e
```

---

## ۷. Deployment Strategy

### ۷.۱ Docker Build

```bash
# Build all services
docker-compose build

# Run locally
docker-compose up
```

### ۷.۲ Production Deployment

```bash
# Build Docker images
docker build -t media-platform:latest .

# Push to registry
docker push your-registry/media-platform:latest

# Deploy to Kubernetes
kubectl apply -f infrastructure/kubernetes/
```

---

## ۸. Performance Optimization Tips

### ۸.۱ Backend
- Use connection pooling (PgBouncer)
- Cache frequently accessed data in Redis
- Implement pagination for large datasets
- Use CDN for media streaming

### ۸.۲ Frontend
- Lazy load components
- Optimize images
- Use React.memo for expensive components
- Implement virtualization for long lists

### ۸.۳ Media
- Implement adaptive bitrate streaming
- Use HLS format for better compatibility
- Compress media before upload
- Use CDN for content delivery

---

## ۹. Common Development Tasks

### ۹.۱ Add New Feature

1. Create API endpoint in service
2. Create database migration if needed
3. Update types in shared package
4. Implement frontend screen/component
5. Write tests
6. Update API documentation

### ۹.۲ Debug Service

```bash
# Check logs
docker-compose logs -f auth-service

# Connect to database
docker-compose exec postgres psql -U user -d media_platform

# Monitor Redis
docker-compose exec redis redis-cli MONITOR
```

### ۹.۳ Reset Development Environment

```bash
# Stop and remove containers
docker-compose down -v

# Remove cache
rm -rf node_modules dist

# Reinstall
yarn install

# Restart
docker-compose up -d
yarn dev
```

---

## ۱۰. API Testing with cURL

### ۱۰.۱ Register User

```bash
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePass123!",
    "firstName": "علی",
    "lastName": "احمدی",
    "username": "ali_ahmadi"
  }'
```

### ۱۰.۲ Login

```bash
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "SecurePass123!"
  }'
```

### ۱۰.۳ Use Token

```bash
curl -X GET http://localhost:3002/api/users/me \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

---

## ۱۱. Frontend API Integration

### ۱۱.۱ React Native Example

```typescript
// services/api.ts
import axios from 'axios';
import * as SecureStore from 'expo-secure-store';

const API_BASE_URL = 'http://localhost:3000/v1';

const api = axios.create({
  baseURL: API_BASE_URL,
});

// Add token to requests
api.interceptors.request.use(async (config) => {
  const token = await SecureStore.getItemAsync('accessToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export default api;
```

### ۱۱.۲ Next.js Example

```typescript
// lib/api.ts
import axios from 'axios';
import { useRouter } from 'next/router';

const api = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
});

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('accessToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

export default api;
```

---

## ۱۲. Version Control Best Practices

### ۱۲.۱ Branch Naming
```
feature/user-authentication
fix/media-streaming-issue
refactor/optimize-database-queries
docs/api-documentation
```

### ۱۲.۲ Commit Messages
```
feat(auth): implement OAuth2 Google login
fix(media): resolve HLS streaming buffer issue
refactor(database): optimize user queries
docs: update API specification
```

### ۱۲.۳ Pull Requests
- Link to issues
- Describe changes
- Add screenshots for UI changes
- Request review from team members

---

**تاریخ آخرین به‌روز رسانی:** ۱۴۰۵/۰۴/۱۳
**نسخه:** ۱.۰
**وضعیت:** Implementation Guide Ready
